package howtoapply.c_misc;

/**
 * *****************************************************************************
 * Reverse a stack without using extra space in O(n)
 * *****************************************************************************
 */
public class A01_reverseStack {
	/*
	 * If we internally represent the stack as inked list.
	 * The problems of "reversing a stack" is now converted to "reversing a linked list",
	 * 		which can be done in O(n)time and O(1)extra space.
	 * 
	 */
}
