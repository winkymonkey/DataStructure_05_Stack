package com.example.datastructure.howtoapply.a_designstack;

/**
 * *****************************************************************************
 * Design and Implement Special Stack Data Structure | Added Space Optimized Version
 * *****************************************************************************
 */
public class A01_designStack_getMin_timeO1 {
	/*
	 * Keep one main stack called "myStack"
	 * Keep another auxiliary stack called "auxStack"
	 * Design a stack in such a way that
	 * 	- whenever you push something in stack, compare "myStack.top" and "newElement"
	 * 	- and push the MIN(myStack.top, newElement) into "auxStack"
	 * 
	 * Whenever we call "myStack.getMin()" return "auxStack.top"
	 *  
	 */
}
